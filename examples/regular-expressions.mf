let strings = [
    "Johnny Appleseed",
    "Some long text that does not match!",
    "Isaac Newton"
];
for s in strings {
    if s =~ r`^(\w+) (\w+)$` {
        println($"First Name = {repr($1)}, Last Name = {repr($2)}");
    }
}
