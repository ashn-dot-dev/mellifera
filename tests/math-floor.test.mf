dumpln(math::floor(NaN));
dumpln(math::floor(+Inf));
dumpln(math::floor(-Inf));
dumpln(math::floor(+123));
dumpln(math::floor(-123));
dumpln(math::floor(+123.456));
dumpln(math::floor(-123.456));
dumpln(math::floor(+123.654));
dumpln(math::floor(-123.654));
################################################################################
# NaN
# Inf
# -Inf
# 123
# -123
# 123
# -124
# 123
# -124
