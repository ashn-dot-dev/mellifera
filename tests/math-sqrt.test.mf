dumpln(math::sqrt(NaN));
dumpln(math::sqrt(+Inf));
try { dumpln(math::sqrt(-Inf)); } catch err { dumpln(err); }

dumpln(math::sqrt(+4));
try { dumpln(math::sqrt(-4)); } catch err { dumpln(err); }
################################################################################
# NaN
# Inf
# "math domain error"
# 2
# "math domain error"
