dumpln(math::sqrt(NaN));
dumpln(math::sqrt(+Inf));
dumpln(math::sqrt(-Inf));

dumpln(math::sqrt(-1));
dumpln(math::sqrt(+0));
dumpln(math::sqrt(+1));
dumpln(math::sqrt(+4));
dumpln(math::sqrt(+9));
################################################################################
# NaN
# Inf
# NaN
# NaN
# 0
# 1
# 2
# 3
