try { {NaN}; } catch err { dumpln(err); }
try { let x = Set{}; x.insert(NaN); } catch err { dumpln(err); }
################################################################################
# "invalid set construction with element NaN"
# "invalid set::insert operation (invalid NaN set element)"
