dumpln(math::trunc(NaN));
dumpln(math::trunc(+Inf));
dumpln(math::trunc(-Inf));
dumpln(math::trunc(+123));
dumpln(math::trunc(-123));
dumpln(math::trunc(+123.456));
dumpln(math::trunc(-123.456));
dumpln(math::trunc(+123.654));
dumpln(math::trunc(-123.654));
################################################################################
# NaN
# Inf
# -Inf
# 123
# -123
# 123
# -123
# 123
# -123
