dumpln(math::acos(NaN));
dumpln(math::acos(+Inf));
dumpln(math::acos(-Inf));
dumpln(math::acos(+1.1));
dumpln(math::acos(-1.1));
dumpln(math::acos(+1).fixed(2));
dumpln(math::acos(+0).fixed(2));
dumpln(math::acos(-1).fixed(2));
################################################################################
# NaN
# NaN
# NaN
# NaN
# NaN
# 0
# 1.57
# 3.14
