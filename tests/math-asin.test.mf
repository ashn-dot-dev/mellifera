dumpln(math::asin(NaN));
dumpln(math::asin(+Inf));
dumpln(math::asin(-Inf));
dumpln(math::asin(+1.1));
dumpln(math::asin(-1.1));
dumpln(math::asin(+0).fixed(2));
dumpln(math::asin(+1).fixed(2));
dumpln(math::asin(-1).fixed(2));
################################################################################
# NaN
# NaN
# NaN
# NaN
# NaN
# 0
# 1.57
# -1.57
