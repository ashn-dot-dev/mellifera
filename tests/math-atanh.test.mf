dumpln(math::atanh(NaN));
dumpln(math::atanh(+Inf));
dumpln(math::atanh(-Inf));
dumpln(math::atanh(0));
dumpln(math::atanh(-0.5).fixed(3));
dumpln(math::atanh(-1));
dumpln(math::atanh(-1.1));
dumpln(math::atanh(+0.5).fixed(3));
dumpln(math::atanh(+1));
dumpln(math::atanh(+1.1));
################################################################################
# NaN
# NaN
# NaN
# 0
# -0.549
# -Inf
# NaN
# 0.549
# Inf
# NaN
