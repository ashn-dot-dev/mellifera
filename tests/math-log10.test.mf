dumpln(math::log10(NaN));
dumpln(math::log10(+Inf));
dumpln(math::log10(-Inf));
dumpln(math::log10(-1));
dumpln(math::log10(0));
dumpln(math::log10(1).fixed(3));
dumpln(math::log10(2).fixed(3));
dumpln(math::log10(8).fixed(3));
dumpln(math::log10(10).fixed(3));
################################################################################
# NaN
# Inf
# NaN
# NaN
# -Inf
# 0
# 0.301
# 0.903
# 1
