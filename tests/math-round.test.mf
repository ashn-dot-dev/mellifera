dumpln(math::round(NaN));
dumpln(math::round(+Inf));
dumpln(math::round(-Inf));
dumpln(math::round(+123));
dumpln(math::round(-123));
dumpln(math::round(+123.456));
dumpln(math::round(-123.456));
dumpln(math::round(+123.654));
dumpln(math::round(-123.654));
dumpln(math::round(+0.5));
dumpln(math::round(-0.5));
dumpln(math::round(+2.5));
dumpln(math::round(-2.5));
dumpln(math::round(+0.49999999999999994));
dumpln(math::round(-0.49999999999999994));
################################################################################
# NaN
# Inf
# -Inf
# 123
# -123
# 123
# -123
# 124
# -124
# 1
# -1
# 3
# -3
# 0
# 0
