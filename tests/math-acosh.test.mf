dumpln(math::acosh(NaN));
dumpln(math::acosh(+Inf));
dumpln(math::acosh(-Inf));
dumpln(math::acosh(1));
dumpln(math::acosh(1.543).fixed(1));
dumpln(math::acosh(-1));
################################################################################
# NaN
# Inf
# NaN
# 0
# 1
# NaN
