println($`["foo", "bar", "baz"].sorted() is {["foo", "bar", "baz"].sorted()}`);

print("\n");

try { vector::sorted(Map{"foo": "bar"}); } catch err { dumpln(err); }
try { vector::sorted(Map{"foo": "bar"}.&); } catch err { dumpln(err); }

print("\n");

try { println($`["foo", 123, "baz"].sorted() is {["foo", 123, "baz"].sorted()}`); } catch err { dumpln(err); }
################################################################################
# ["foo", "bar", "baz"].sorted() is ["bar", "baz", "foo"]
#
# "expected vector value for argument 1, received map"
# "expected vector value for argument 1, received reference"
#
# "attempted < operation with types `number` and `string`"
