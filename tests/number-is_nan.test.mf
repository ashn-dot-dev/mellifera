println($`123.is_nan() is {123.is_nan()}`);
println($`Inf.is_nan() is {Inf.is_nan()}`);
println($`NaN.is_nan() is {NaN.is_nan()}`);
################################################################################
# 123.is_nan() is false
# Inf.is_nan() is false
# NaN.is_nan() is true
