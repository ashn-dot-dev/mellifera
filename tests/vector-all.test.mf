dumpln([].all(function(x) { return x < 5; }));
dumpln([1, 2, 3].all(function(x) { return x < 5; }));
dumpln([1, 2, 3, 4].all(function(x) { return x < 5; }));
dumpln([1, 2, 3, 4, 5].all(function(x) { return x < 5; }));

print("\n");

let boom = function(x) { return "boom"; };
try { [1, 2, 3].all(boom); } catch err { dumpln(err); }
################################################################################
# true
# true
# true
# false
#
# "expected function boom@[vector-all.test.mf, line 8] to return a boolean (received string \"boom\")"
