dumpln(math::log2(NaN));
dumpln(math::log2(+Inf));
dumpln(math::log2(-Inf));
dumpln(math::log2(-1));
dumpln(math::log2(0));
dumpln(math::log2(1).fixed(3));
dumpln(math::log2(2).fixed(3));
dumpln(math::log2(8).fixed(3));
dumpln(math::log2(10).fixed(3));
################################################################################
# NaN
# Inf
# NaN
# NaN
# -Inf
# 0
# 1
# 3
# 3.322
