dumpln([1, 3, 5].into_iterator().reduce(function(accumulator, value) {
    return accumulator + value;
}));
dumpln([42].into_iterator().reduce(function(accumulator, value) {
    return accumulator + value;
}));

print("\n");

try { [1, 3, 5].into_iterator().reduce(function(nope) { }); } catch err { dumpln(err); }
try {
    [].into_iterator().reduce(function(accumulator, value) {
        return accumulator + value;
    });
} catch err {
    dumpln(err);
}
try {
    [1, "foo"].into_iterator().reduce(function(accumulator, value) {
        return accumulator + value;
    });
} catch err {
    dumpln(err);
}
################################################################################
# 9
# 42
#
# "invalid function argument count (expected 1, received 2)"
# "attempted iterator::reduce on an exhausted iterator"
# "attempted + operation with types `number` and `string`"
