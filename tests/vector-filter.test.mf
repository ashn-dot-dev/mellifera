for x in [1, 2, 3].filter(function(x) { return x % 2 != 0; }) {
    dumpln(x);
}

print("\n");

let boom = function(x) { return "boom"; };
try { [1, 2, 3].filter(boom); } catch err { dumpln(err); }
################################################################################
# 1
# 3
#
# "expected function boom@[vector-filter.test.mf, line 7] to return a boolean (received string \"boom\")"
