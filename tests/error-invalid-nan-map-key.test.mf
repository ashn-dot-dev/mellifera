try { {NaN: "foo"}; } catch err { dumpln(err); }
try { let x = Map{}; x[NaN] = "foo"; } catch err { dumpln(err); }
################################################################################
# "invalid map construction with key NaN and value \"foo\""
# "invalid map assignment with key NaN (invalid NaN map key)"
